import 'package:flutter/material.dart';

class GuestAppointment extends StatelessWidget {
  const GuestAppointment({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
